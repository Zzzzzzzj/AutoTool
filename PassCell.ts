export class PassCell{
  uid:string// 格子唯一ID（`${mapId}_${id}_${x}_${y}`）;
  status:number// 0未领取 1已领取 2待领取;
  star:number// 星数;
  times:number// 今日攻打次数;
  info:PassCell;
  passMapId:number;
  passMapId:number// 最后进入的地图ID;
  passMapId:number;
  mapId:number;
  uid:string// 格子唯一ID;
  star:number// 新的格子星数;
  uid:string// 格子唯一ID;
  times:number// 次数;
  mapId:number// 地图ID;
  id:number// 星数;
}