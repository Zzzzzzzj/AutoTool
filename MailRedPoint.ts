export class MailRedPoint{
  type:number// 邮件类型;
  num:number// <0表示有邮件没红点，0表示没邮件没红点，>0表示红点数;
  type:number;
  maxUid:number// 已有邮件列表的最小唯一id;
  _id:number// 邮件唯一id;
  _id:number// 邮件唯一id;
  type:number// 邮件类型;
  type:number// 邮件类型;
  _id:number// 有值，表示删除单个邮件;
}